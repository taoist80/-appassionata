# Copy to .env.local and fill in. All vars are build-time (Vite, VITE_ prefix).

# GA4 Measurement ID (e.g. G-XXXXXXXXXX). Analytics is a NO-OP when this is unset.
# Create a property at https://analytics.google.com, then paste the ID here.
VITE_GA4_ID=
